import PageContent from '@/pages/ResetPassword';

export const metadata = { robots: { index: false, follow: false }, alternates: { canonical: '/reset-password' } };

export default function Page() { return <PageContent />; }
