name: Final Homefort Beds Scrape

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/scrape-homefort-beds-final.yml'

permissions:
  contents: write

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install scraper dependencies
        run: pip install requests beautifulsoup4 lxml
      - name: Scrape every real bed product
        run: python scripts/scrape_homefort_beds_v4.py
      - name: Commit final dataset and clean workflow
        run: |
          rm -f .github/workflows/scrape-homefort-beds-final.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data/homefort-beds.json .github/workflows/scrape-homefort-beds-final.yml
          git commit -m "data: complete Homefort beds catalog"
          git pull --rebase origin main
          git push origin main
